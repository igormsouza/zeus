@model Client.Zeus.Dominio.TB_UF

<h2>UF</h2>
<div class="row">
    <div class="col-md-8">

   @using (Html.BeginForm("Create", "UF", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        <fieldset>
            @Html.AntiForgeryToken()

            <div class="form-group">
                @Html.LabelFor(m => m.NOME, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.NOME, new { @class = "form-control", maxlength = "50" })
                    @Html.ValidationMessageFor(m => m.NOME, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.SIGLA, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.SIGLA, new { @class = "form-control", maxlength = "2" })
                    @Html.ValidationMessageFor(m => m.SIGLA, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-4">
                    <button type="submit"value="Create" class="btn btn-default">
                        <span class="glyphicon glyphicon-search"></span>&nbsp;Cadastrar
                    </button>
                </div>
            </div>
        </fieldset>
    }

    <div>
        @Html.ActionLink("Voltar", "Index")
    </div>

    </div>
</div>
   
